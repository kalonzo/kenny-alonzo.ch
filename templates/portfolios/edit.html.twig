{% extends 'base.html.twig' %}

{% block title %}Edit Portfolios{% endblock %}

{% block body %}
    <h1>Edit Portfolios</h1>

    {{ include('portfolios/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('portfolios_index') }}">back to list</a>

    {{ include('portfolios/_delete_form.html.twig') }}
{% endblock %}
