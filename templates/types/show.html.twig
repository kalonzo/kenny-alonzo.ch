{% extends 'base.html.twig' %}

{% block title %}Types{% endblock %}

{% block body %}
    <h1>Types</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdType</th>
                <td>{{ type.idType }}</td>
            </tr>
            <tr>
                <th>Label</th>
                <td>{{ type.label }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('types_index') }}">back to list</a>

    <a href="{{ path('types_edit', {'idType': type.idType}) }}">edit</a>

    {{ include('types/_delete_form.html.twig') }}
{% endblock %}
