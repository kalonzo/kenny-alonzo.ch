<form method="post" action="{{ path('hobbies_delete', {'idHobbie': hobby.idHobbie}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ hobby.idHobbie) }}">
    <button class="btn">Delete</button>
</form>
