{% extends 'base.html.twig' %}

{% block title %}Hobbies{% endblock %}

{% block body %}
    <h1>Hobbies</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdHobbie</th>
                <td>{{ hobby.idHobbie }}</td>
            </tr>
            <tr>
                <th>CreationDate</th>
                <td>{{ hobby.creationDate ? hobby.creationDate|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('hobbies_index') }}">back to list</a>

    <a href="{{ path('hobbies_edit', {'idHobbie': hobby.idHobbie}) }}">edit</a>

    {{ include('hobbies/_delete_form.html.twig') }}
{% endblock %}
