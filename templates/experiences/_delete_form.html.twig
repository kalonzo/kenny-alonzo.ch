<form method="post" action="{{ path('experiences_delete', {'idExperience': experience.idExperience}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ experience.idExperience) }}">
    <button class="btn">Delete</button>
</form>
